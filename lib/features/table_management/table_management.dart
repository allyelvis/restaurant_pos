import 'package:flutter/material.dart';

class TableManagement extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Table Management'),
      ),
      body: Center(
        child: Text('Manage restaurant tables here'),
      ),
    );
  }
}
