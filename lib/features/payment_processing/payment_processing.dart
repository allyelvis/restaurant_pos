import 'package:flutter/material.dart';

class PaymentProcessing extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Payment Processing'),
      ),
      body: Center(
        child: Text('Process payments here'),
      ),
    );
  }
}
