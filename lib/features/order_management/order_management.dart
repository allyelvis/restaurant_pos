import 'package:flutter/material.dart';

class OrderManagement extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Order Management'),
      ),
      body: Center(
        child: Text('Manage orders here'),
      ),
    );
  }
}
